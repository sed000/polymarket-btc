# Wallet private key (required)
PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Polymarket API credentials (get from polymarket.com developer settings)
POLY_API_KEY=your-api-key
POLY_API_SECRET=your-api-secret
POLY_API_PASSPHRASE=your-api-passphrase

# Strategy settings (prices in dollars, e.g., 0.95 = $0.95)
ENTRY_THRESHOLD=0.95    # Buy when price >= this (min entry)
MAX_ENTRY_PRICE=0.98    # Don't buy above this (avoid ceiling)
STOP_LOSS=0.80          # Sell when price drops to this level
STOP_LOSS_DELAY_MS=5000 # Wait 5 seconds before confirming stop-loss (filters false triggers)
MAX_SPREAD=0.03         # Skip markets with bid-ask spread > $0.03
TIME_WINDOW_MINS=5      # Only enter within 5 minutes of expiry

# Risk mode: "normal" or "super-risk"
# normal: Entry $0.95-0.98, Stop $0.80, 5min window
# super-risk: Entry $0.70-0.95, Stop $0.40, no time limit (HIGH RISK!)
RISK_MODE=normal

# Bot settings
POLL_INTERVAL_MS=10000  # Check markets every 10 seconds

# Paper trading (simulation mode)
PAPER_TRADING=false     # Set to true to simulate trades with virtual money
PAPER_BALANCE=100       # Starting balance for paper trading
